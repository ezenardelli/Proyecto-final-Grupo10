<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head.ejs') %>
    <body>
        <main>
            <h2>List of Products</h2>
    <ul>
        <% for(let i=0; i< products.length; i++) {%>
            <li>
                <article class="card">
                    <img src="<%= products[i].image %>" alt="">
                    <p>
                        <a href="/user/<%= products[i].id %>">
                            Name: <%= products[i].name %>
                        </a>
                    </p>
                </article>
            </li>
        <%}%>
    </ul>
        </main>
    </body>
</html>